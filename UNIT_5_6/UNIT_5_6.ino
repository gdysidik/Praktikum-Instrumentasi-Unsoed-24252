#include "IR.h"
#include "ldr.h"
#include "mic.h"
#include "ul_so.h"

void setup() {
  // set_IR();
  // set_ldr();
  set_mic();
  // set_ul_so();
}

void loop() {
  // run_IR();
  // run_ldr();
  run_mic();
  // run_ul_so();
}